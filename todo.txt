abstract all navigation

add debug/reset menu options
	Add the ability to reset all data accumulated






charting total time spent - 
	check box to disable/enable display all schedules
	check box to compare time not spent (auto-disables display all schedules)
	
chart types
	add line chart
	add pie chart
	add scatterplot for complex datasets
	
determine if months are supposed to start with 0
	adjust display to show day of the week and month in words
	check user inputs for mm/dd/yyyy
	look into specialized inputs

move month lookup from chart and checker classes to utilties

if system crashes while program is running, previous.txt can be erased causing an error




Add ability to log new entries while program is open (after getting past the initial input entry phase)

Add the ability to silence reminders

import/export schedules and corresponding data(optional)